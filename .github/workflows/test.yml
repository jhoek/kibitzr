name: test

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        shell: pwsh
        run: |
            $MyDateTime = Get-Date -Day 1 -Month 6
            Write-Host ($MyDateTime.ToString('d MMM'))
