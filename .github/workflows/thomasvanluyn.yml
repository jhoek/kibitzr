name: thomasvanluyn

on:
  schedule:
    - cron: "25 8 * * 4"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install pup
        shell: bash
        run: |
          go get github.com/ericchiang/pup
          export GOROOT=/usr/bin/go
          cd ~/go/bin
          sudo cp pup /usr/bin
          pup --version

      - name: Install required PowerShell modules
        shell: pwsh
        run: |
          Set-PSRepository PSGallery -InstallationPolicy Trusted
          Install-Module UncommonSense.Volkskrant, UncommonSense.Rss -Scope CurrentUser

      - name: Get Recent data
        shell: pwsh
        run: |
          Get-ThomasVanLuyn
          | Select-Object -First 10
          | ForEach-Object {
            New-RssFeedItem `
              -ID $_.Url `
              -Title $_.Title `
              -Description ($_.Body -join "`n") `
              -PubDate $_.Date }
          | New-RssFeed `
            -Title 'Thomas van Luyn' `
            -Link 'https://www.volkskrant.nl/auteur/thomas-van-luyn' `
            -Description 'Thomas van Luyn'
          | Set-Content -Path ./data/thomasvanluyn.xml

      - name: Commit and push feed
        shell: bash
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git commit -m "generated"
          git push
